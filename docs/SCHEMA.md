# Project schema (index)

The full **reference document** (database, `OcrResult`, APIs) lives in one file:

- **[`Project-Schema-Document.md`](Project-Schema-Document.md)** — **Markdown** — use in your repo, or open in **Microsoft Word** / Google Docs (import) / any editor; GitHub renders the tables and mermaid.
- **[`Project-Schema-Document.html`](Project-Schema-Document.html)** — **Print or PDF** — open in a browser, then *Print* → *Save as PDF*.

**Machine-readable API types:** with the app running, see `/openapi.json` and `/docs` (Swagger).
