# Leave empty in dev when using Vite proxy (/api -> backend), especially for phone-on-LAN.
# Set only when API is on a separate origin.
# VITE_API_URL=http://127.0.0.1:8000
# Optional: map preview in LocationPanel (enable Maps Embed API in Google Cloud)
VITE_GOOGLE_MAPS_API_KEY=
